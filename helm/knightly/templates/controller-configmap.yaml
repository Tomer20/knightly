{{- if .Values.controller.configMap.data -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "knightly.controllerFullname" . }}
  labels:
    {{- include "knightly.controllerLabels" . | nindent 4 }}
data:
  {{- range $name, $value := .Values.controller.configMap.data }}
    {{ $name }}: {{ $value | quote }}
  {{- end }}
{{- end -}}
